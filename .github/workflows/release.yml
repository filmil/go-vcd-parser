# Monthly and on-demand binary releases.
#
# The version comes from conventional commits: fix: bumps the patch, feat:
# the minor, BREAKING CHANGE the major. A month with no fix or feat commits
# produces no tag and no release, so re-running the schedule against an
# unchanged tree is a no-op. `default_bump: false` is what enforces that;
# without it the action invents a patch bump on every run.
#
# The binaries are cross-compiled from one Linux runner with the zig-based
# hermetic C toolchain declared in MODULE.bazel. Both programs link
# mattn/go-sqlite3, so every build is a CGO build; nothing is taken from the
# runner but bazelisk itself.
name: Release
on:
  schedule:
    - cron: "17 6 5 * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup bazel
        uses: bazel-contrib/setup-bazel@0.15.0
        with:
          bazelisk-cache: true
          disk-cache: ${{ github.workflow }}
          repository-cache: true

      - name: Compute the next version
        id: next
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          release_branches: main
          default_bump: false
          dry_run: true

      # The action above answers nothing when the repository has no tag at
      # all, so the very first release needs its version stated here. Every
      # later release computes normally from the tag this one plants.
      - name: Resolve the release tag
        id: tag
        run: |
          tag="${{ steps.next.outputs.new_tag }}"
          if [ -z "$tag" ] && [ -z "$(git tag)" ]; then
            tag="v0.1.0"
            echo "no tags exist; bootstrapping the first release as $tag"
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"

      - name: Build the release binaries
        if: steps.tag.outputs.tag != ''
        run: |
          mkdir -p out
          for platform in linux_amd64 linux_arm64 darwin_amd64 darwin_arm64; do
            bazel build --platforms=@zig_sdk//platform:"$platform" //bin/...
            os="${platform%_*}"
            arch="${platform#*_}"
            for name in vcdcvt sqlite2drawtiming; do
              cp "bazel-bin/bin/$name/${name}_/$name" "out/${name}-${os}-${arch}"
            done
          done
          (cd out && sha256sum -- * > SHA256SUMS)
          ls -la out/

      # `gh release create` makes the tag itself, so no separate tagging
      # step: one writer, and a build failure above leaves no orphan tag.
      - name: Release
        if: steps.tag.outputs.tag != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ steps.tag.outputs.tag }}" \
            --target "$GITHUB_SHA" \
            --title "${{ steps.tag.outputs.tag }}" \
            --generate-notes \
            out/*
